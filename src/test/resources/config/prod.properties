# ── Production (smoke only) environment overrides ────────────
app.base.url=https://app.yourcompany.com
browser=edge
headless=true
wait.explicit=30
screenshot.on.pass=false
# WARNING: only @smoke tagged tests should run in production
